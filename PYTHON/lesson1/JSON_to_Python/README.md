# 🐍 JSON ↔ Python Conversion Example  
Repository: [HexSecGuru/IEKAKMH](https://github.com/HexSecGuru/IEKAKMH)

A simple Python example demonstrating how to:
- Convert between **Python dictionaries** and **JSON strings**
- Save JSON data to a file
- Read JSON data back into Python

---

## 📘 Description

This script shows how to work with JSON data in Python using the built-in `json` module.  
It covers:
1. Converting a Python dictionary to a JSON string.
2. Converting a JSON string back to a Python dictionary.
3. Writing a JSON object to a `.json` file.
4. Reading that file back into Python.

This is a **beginner-friendly** example to understand data serialization and deserialization.

---

## 🧠 What You’ll Learn

- How to use the `json` module
- Convert:
  - **Python → JSON** with `json.dumps()` and `json.dump()`
  - **JSON → Python** with `json.loads()` and `json.load()`
- Safely handle file I/O with `with open(...)`
- Understand how Python objects and JSON structures map to each other

---

## 🚀 How to Run

1. **Clone this repository**
   ```bash
   git clone https://github.com/HexSecGuru/IEKAKMH.git
   cd IEKAKMH
